import { getAds, getNews } from '../api';
import { escapeHtml } from '../main';
import { categoryInfo, PRODUCT_CATEGORIES } from '../categories';
const card = (ad: any) => { const category = categoryInfo(ad.Type, ad.Category); return `<a class="ad-card" href="#/ads/${ad.ID}"><div class="ad-image">${ad.ImageURL ? `<img src="${escapeHtml(ad.ImageURL)}" alt="">` : `<span class="ad-image-emoji">${category.icon}</span>`}</div><div class="ad-card-body"><div class="card-meta"><span class="tag">${escapeHtml(category.name)}</span>${ad.Type === 'service' ? '<span class="type-tag">Услуга</span>' : ''}</div><h3>${escapeHtml(ad.Title)}</h3><strong class="price">${ad.Price ? `${ad.Price.toLocaleString('ru-RU')} ₽` : 'Цена по договорённости'}</strong></div></a>`; };
export async function renderHome() { try { const [ads, news] = await Promise.all([getAds({ type: 'product' }), getNews()]); const categories = PRODUCT_CATEGORIES.slice(0, 8); return `<section class="hero-shell"><div class="hero panel"><div class="hero-copy"><span class="eyebrow">Городская площадка</span><h1>Найди своё рядом.</h1><p>Покупай, продавай и находи проверенные услуги без лишнего шума.</p><div class="hero-actions"><a class="button primary" href="#/catalog">Смотреть каталог</a><a class="button secondary" href="#/ad/new">+ Подать объявление</a></div></div><div class="hero-showcase"><div class="mini-stat"><span>Сегодня</span><strong>${ads.length}</strong><small>свежих товаров</small></div><div class="mini-stat"><span>Категорий</span><strong>12+</strong><small>в каталоге</small></div><div class="mini-stat"><span>Сервис</span><strong>24/7</strong><small>самовывоз/услуги</small></div></div></div></section><section class="section-block"><div class="section-head"><div><span class="eyebrow">Категории</span><h2>Проверь интересное</h2></div><a class="text-link" href="#/catalog">Все категории →</a></div><div class="category-grid compact">${categories.map((category) => `<a class="category-tile" href="#/catalog/category/${category.slug}"><span class="category-icon">${category.icon}</span><span>${escapeHtml(category.name)}</span></a>`).join('')}</div></section><section class="section-block"><div class="section-head"><div><span class="eyebrow">Подборка</span><h2>Свежие объявления</h2></div><div class="section-links"><a class="text-link" href="#/catalog">Все товары →</a> <a class="text-link" href="#/catalog/services">Услуги →</a></div></div><div class="ad-grid">${ads.slice(0, 4).map(card).join('')}</div></section><section class="section-block"><div class="section-head"><div><span class="eyebrow">Журнал</span><h2>Новости площадки</h2></div><a class="text-link" href="#/news">Все новости →</a></div><div class="news-list">${news.slice(0, 2).map((item) => `<a class="news-item" href="#/news/${item.ID}"><time>${item.CreatedAt.slice(0, 10)}</time><h3>${escapeHtml(item.Title)}</h3><p>${escapeHtml(item.Content.slice(0, 120))}</p></a>`).join('')}</div></section>`; } catch (error) { return `<div class="error">${escapeHtml((error as Error).message)}</div>`; } }
export { card };
